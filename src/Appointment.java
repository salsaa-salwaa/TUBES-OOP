public class Appointment {
    // Atribut: appointmentId, patientId, doctorId, appointmentDate, status(Scheduled, Cancelled, Completed)
    //updateStatus(String newStatus): ubah status appointment
    //Getter dan setter atribut
   
}
